# Missing Materials

This file tracks materials referenced by the known PDF skill docs but not yet recovered or implemented locally.

The full recovered script path inventory is recorded in `docs/recovered-script-heads.md`. Function/argument metadata is recorded in `docs/script-inventory.json`.

## Missing task docs

None currently known from the recovered package map.

## Missing Python scripts

None currently known from the recovered package map.

## Missing Node helpers

None currently known from the recovered package map.

## Missing examples and troubleshooting

None currently known from the recovered package map.

## Platform components that can only be approximated locally

These are not missing PDF package files. They are ChatGPT platform services referenced by the leaked environment prompt. The local OpenCode replacement map is documented in `docs/platform-local-replacements.md`.

- `file_search`
- `web.run`
- `web.run.screenshot`
- `python_user_visible`
- `artifact_tool`
- ChatGPT upload indexing and `sandbox:/mnt/data/...` citation/link generation
